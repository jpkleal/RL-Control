# The 3 project repos are cloned here by setup.sh - they are independent
# git repositories with their own remotes/history, not part of this repo.
/LyNCh/
/RL-Engine/
/NeonFC-SSL/

# Real data directories backing the cross-project symlinks (shared/results,
# shared/models) - generated by setup.sh, not something to version here.
/shared/

# Per-service run logs, generated by run.sh
/logs/

# Common local cruft
.DS_Store
*.log
